// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::ability_type::Ability;
use super::caster_progression_type::CasterProgression;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SeedDnd5EClassArgs {
    pub name: String,
    pub source: String,
    pub edition: Option<String>,
    pub hit_die: u8,
    pub saving_throws: Vec<Ability>,
    pub spellcasting_ability: Option<Ability>,
    pub caster_progression: Option<CasterProgression>,
    pub prepared_spells_formula: Option<String>,
    pub prepared_spells_progression: Vec<u8>,
    pub cantrip_progression: Vec<u8>,
    pub class_features: Vec<String>,
    pub subclass_title: Option<String>,
}

impl From<SeedDnd5EClassArgs> for super::Reducer {
    fn from(args: SeedDnd5EClassArgs) -> Self {
        Self::SeedDnd5EClass {
            name: args.name,
            source: args.source,
            edition: args.edition,
            hit_die: args.hit_die,
            saving_throws: args.saving_throws,
            spellcasting_ability: args.spellcasting_ability,
            caster_progression: args.caster_progression,
            prepared_spells_formula: args.prepared_spells_formula,
            prepared_spells_progression: args.prepared_spells_progression,
            cantrip_progression: args.cantrip_progression,
            class_features: args.class_features,
            subclass_title: args.subclass_title,
        }
    }
}

impl __sdk::InModule for SeedDnd5EClassArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `seed_dnd_5_e_class`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait seed_dnd_5_e_class {
    /// Request that the remote module invoke the reducer `seed_dnd_5_e_class` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`seed_dnd_5_e_class:seed_dnd_5_e_class_then`] to run a callback after the reducer completes.
    fn seed_dnd_5_e_class(
        &self,
        name: String,
        source: String,
        edition: Option<String>,
        hit_die: u8,
        saving_throws: Vec<Ability>,
        spellcasting_ability: Option<Ability>,
        caster_progression: Option<CasterProgression>,
        prepared_spells_formula: Option<String>,
        prepared_spells_progression: Vec<u8>,
        cantrip_progression: Vec<u8>,
        class_features: Vec<String>,
        subclass_title: Option<String>,
    ) -> __sdk::Result<()> {
        self.seed_dnd_5_e_class_then(
            name,
            source,
            edition,
            hit_die,
            saving_throws,
            spellcasting_ability,
            caster_progression,
            prepared_spells_formula,
            prepared_spells_progression,
            cantrip_progression,
            class_features,
            subclass_title,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `seed_dnd_5_e_class` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn seed_dnd_5_e_class_then(
        &self,
        name: String,
        source: String,
        edition: Option<String>,
        hit_die: u8,
        saving_throws: Vec<Ability>,
        spellcasting_ability: Option<Ability>,
        caster_progression: Option<CasterProgression>,
        prepared_spells_formula: Option<String>,
        prepared_spells_progression: Vec<u8>,
        cantrip_progression: Vec<u8>,
        class_features: Vec<String>,
        subclass_title: Option<String>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl seed_dnd_5_e_class for super::RemoteReducers {
    fn seed_dnd_5_e_class_then(
        &self,
        name: String,
        source: String,
        edition: Option<String>,
        hit_die: u8,
        saving_throws: Vec<Ability>,
        spellcasting_ability: Option<Ability>,
        caster_progression: Option<CasterProgression>,
        prepared_spells_formula: Option<String>,
        prepared_spells_progression: Vec<u8>,
        cantrip_progression: Vec<u8>,
        class_features: Vec<String>,
        subclass_title: Option<String>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SeedDnd5EClassArgs {
                name,
                source,
                edition,
                hit_die,
                saving_throws,
                spellcasting_ability,
                caster_progression,
                prepared_spells_formula,
                prepared_spells_progression,
                cantrip_progression,
                class_features,
                subclass_title,
            },
            callback,
        )
    }
}
