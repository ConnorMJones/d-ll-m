// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::ability_type::Ability;
use super::spell_school_type::SpellSchool;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SeedDnd5ESpellArgs {
    pub name: String,
    pub source: String,
    pub level: u8,
    pub school: SpellSchool,
    pub ritual: bool,
    pub concentration: bool,
    pub description: String,
    pub saving_throw: Option<Ability>,
}

impl From<SeedDnd5ESpellArgs> for super::Reducer {
    fn from(args: SeedDnd5ESpellArgs) -> Self {
        Self::SeedDnd5ESpell {
            name: args.name,
            source: args.source,
            level: args.level,
            school: args.school,
            ritual: args.ritual,
            concentration: args.concentration,
            description: args.description,
            saving_throw: args.saving_throw,
        }
    }
}

impl __sdk::InModule for SeedDnd5ESpellArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `seed_dnd_5_e_spell`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait seed_dnd_5_e_spell {
    /// Request that the remote module invoke the reducer `seed_dnd_5_e_spell` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`seed_dnd_5_e_spell:seed_dnd_5_e_spell_then`] to run a callback after the reducer completes.
    fn seed_dnd_5_e_spell(
        &self,
        name: String,
        source: String,
        level: u8,
        school: SpellSchool,
        ritual: bool,
        concentration: bool,
        description: String,
        saving_throw: Option<Ability>,
    ) -> __sdk::Result<()> {
        self.seed_dnd_5_e_spell_then(
            name,
            source,
            level,
            school,
            ritual,
            concentration,
            description,
            saving_throw,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `seed_dnd_5_e_spell` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn seed_dnd_5_e_spell_then(
        &self,
        name: String,
        source: String,
        level: u8,
        school: SpellSchool,
        ritual: bool,
        concentration: bool,
        description: String,
        saving_throw: Option<Ability>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl seed_dnd_5_e_spell for super::RemoteReducers {
    fn seed_dnd_5_e_spell_then(
        &self,
        name: String,
        source: String,
        level: u8,
        school: SpellSchool,
        ritual: bool,
        concentration: bool,
        description: String,
        saving_throw: Option<Ability>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SeedDnd5ESpellArgs {
                name,
                source,
                level,
                school,
                ritual,
                concentration,
                description,
                saving_throw,
            },
            callback,
        )
    }
}
